This "prototyping" folder is for testing out new sensors and sensor configurations before integrating them into the main firmware.

One key thing is that our hardware might be different so we need to be aware of our hardware. This folder is used for simple working code to test out modular small parts of the enirety of the ican system. 

Task 1: 
testing out the sensing for two ultrasonic sensors (HC-SR04),  the ESP32-Wroom-32 and 2 LEDS. Each LED is representing a left and right haptic motor. 